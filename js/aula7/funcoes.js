function calcularMedia (nota1 = 0, nota2 = 0) // passa parametro e set valor default caso não passe um dos parametros
{
    let media = (nota1 + nota2) / 2;
    console.log(media);
}


calcularMedia(10,)